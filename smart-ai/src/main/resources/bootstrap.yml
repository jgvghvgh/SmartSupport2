spring:
  application:
    name: smart-ai
  cloud:
    nacos:
      discovery:
        ip:
        server-addr:   # Nacos 地址
        username: nacos               # 如果 Nacos 设置了用户名密码
        password: nacos
      config:
        server-addr:  # Nacos 配置中心地址
        file-extension: yml
        name: smart-ai   # Data ID，不带后缀
        group: DEFAULT_GROUP