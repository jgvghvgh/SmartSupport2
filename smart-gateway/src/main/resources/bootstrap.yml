spring:
  application:
    name: smart-gateway
  cloud:
    nacos:
      discovery:
        ip:
        server-addr:   # Nacos 地址
        username: nacos               # 如果 Nacos 设置了用户名密码
        password: nacos
        enabled: true
        register-enabled: true
